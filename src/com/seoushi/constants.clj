
(ns com.seoushi.constants
  ;(:require )
  ;(:use )
  ;(:import )
  )


(def gravity    200) ;; 150 pixels a second
(def jump-time  250) ;; 400 milliseconds of jumping up
(def jump-hang-time  75) ;; 75 milliseconds of hanging at the top of the jump
(def world-plane 368) ;; defines the default ground level
